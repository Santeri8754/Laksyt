tuuma = float(input("Anna tuuma: "))
negatiivinen = float(0)
while tuuma >= negatiivinen:
    tuuma_cm = float(tuuma*2.54)
    print(f" Vastaus on {tuuma_cm}")
    tuuma = float(input("Anna tuuma: "))